import React from "react";
import PropTypes from "prop-types";
import styled from "styled-components";
import { Box as GrommetBox } from "grommet";

/**
 * __DESCRIBE A COMPONENT__
 *
 */

const Box = (props) => <GrommetBox {...props} />;

export default Box;
